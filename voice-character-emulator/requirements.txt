fastapi==0.104.1
uvicorn[standard]==0.24.0
pyyaml==6.0.1
python-multipart==0.0.6

# Audio processing
librosa==0.10.0
scipy==1.11.4
soundfile==0.12.1
numpy==1.24.3

# TTS — XTTS-v2 via coqui-tts
# IMPORTANT: PyTorch must be installed FIRST (see SETUP.md)
coqui-tts==0.27.4

# Optional: Personality rewrite via Claude API
# (only if ANTHROPIC_API_KEY in .env)
anthropic==0.25.0

# Optional: Audio enhancement for reference clips
# For MVP, using librosa's built-in functionality
